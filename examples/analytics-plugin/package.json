{
  "name": "analytics-plugin",
  "version": "1.0.0",
  "description": "Custom analytics plugin example for Invoice Liquidity Network SDK",
  "main": "index.ts",
  "scripts": {
    "test": "vitest run",
    "dev": "ts-node index.ts"
  },
  "dependencies": {
    "@iln/sdk": "file:../../sdk"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
